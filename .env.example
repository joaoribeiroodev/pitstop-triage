# OpenAI — obrigatória para /api/gerar-diagnostico e /api/refinar-triagem
OPENAI_API_KEY=

# Opcional — padrão: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Opcional — porta do servidor local de API (padrão: 3000)
API_DEV_PORT=3000
